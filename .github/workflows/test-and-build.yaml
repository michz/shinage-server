name: Test and Build
on:
  push: ~
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04

    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: yes
          MYSQL_DATABASE: shinage_test
        ports:
          - 13306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          tools: symfony

      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Validate composer files
        run: composer validate --no-check-all --strict

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-scripts

      - name: Install yarn dependencies
        run:  |
          corepack enable
          yarn install --frozen-lockfile

      - name: Build assets
        run: ./node_modules/.bin/gulp
        env:
          NODE_ENV: production

      - name: Setup test environment
        shell: bash
        run: |
          cp .env.github-actions .env &&
          cp .env.github-actions .env.test &&
          php bin/console assets:install --env=test &&
          php bin/console doctrine:migrations:migrate -n --env=test &&
          php bin/console doctrine:fixtures:load -n --env=test

      - name: Start test webserver
        run: symfony local:server:start --no-tls --daemon --port=8000

      #- name: Install assets for test environment

      - name: Check PHP codestyle
        run: ./vendor/bin/ecs check --no-progress-bar --ansi --no-interaction

      - name: Check JS codestyle
        run: ./node_modules/.bin/gulp eslint

      - name: Lint YAML files
        run: ./bin/console lint:yaml config/ src/ translations/

      - name: Lint TWIG files
        run: ./bin/console lint:twig config/ src/ templates/

      - name: Check composer dependencies
        run: ./vendor/bin/composer-dependency-analyser
        continue-on-error: true

      - name: Run phpstan
        run: ./vendor/bin/phpstan analyse --no-progress --no-interaction

      - name: Run phpspec
        run: ./vendor/bin/phpspec run --format=pretty --no-code-generation

      - name: Run behat
        run: ./vendor/bin/behat --format=progress --strict -n --tags="~@todo"
        env:
          APP_ENV: test

  package:
    if: github.event_name == 'push' && contains(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-22.04
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'

      - name: Checkout code
        uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Print versions
        run: |
          corepack enable
          php --version
          node --version
          corepack -v
          yarn --version

      - name: Install php dependencies
        run: |
          composer install --prefer-dist --no-progress --no-scripts --no-dev
          composer dump-autoload --classmap-authoritative

      - name: Install yarn dependencies
        run: |
          corepack enable
          yarn install --frozen-lockfile

      - name: Build assets
        run: ./node_modules/.bin/gulp
        env:
          NODE_ENV: production

      - name: Write revision file
        run: echo "${GITHUB_REF#refs/*/}" > REVISION

      - name: Prepare and cleanup
        run: |
          mkdir ./build
          rm -f .gitignore .web-server-pid .env .env.* .eslintrc .nvmrc
          rm -f bin/devEnv.sh bin/runInDev.sh
          rm -f ecs.php package* behat.yml codeception.yml install-git-hooks.php phpspec.yml gulpfile.js
          rm -rf .github/ node_modules/  var/cache/dev/ var/cache/test/ var/session/dev/ var/session/test/ .git/ etc/ features/ tests/ git-hooks/ spec/ data/pool/*

      - name: Package release file
        run: tar --exclude="./build" --exclude="./var/log/*.log" --transform 's,^\./,,' -czf ./build/shinage-server-release.tar.gz .

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref }}
          body: Release ${{ github.ref }}
          draft: false
          prerelease: false
          files: build/shinage-server-release.tar.gz

  docker:
    #if: github.event_name == 'push' && contains(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Print versions
        run: |
          corepack enable
          node --version
          corepack -v
          yarn --version

      - name: Install yarn dependencies
        run: |
          corepack enable
          yarn install --frozen-lockfile

      - name: Build assets
        run: ./node_modules/.bin/gulp
        env:
          NODE_ENV: production

      - name: Write revision file
        run: echo "${{ github.sha }}" > REVISION

      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date -u -Iseconds)"

      - name: Build Docker image
        run: |
          docker buildx build \
            --build-arg "${{ steps.date.outputs.date }}" \
            --build-arg "${{ github.sha }}" \
            --target prod \
            -t "ghcr.io/michz/shinage-server-app:${{ github.sha }}" \
            -t "ghcr.io/michz/shinage-server-app:${{ github.ref_name }}" \
            -f Dockerfile .
          docker buildx build \
            --build-arg "${{ steps.date.outputs.date }}" \
            --build-arg "${{ github.sha }}" \
            -t "ghcr.io/michz/shinage-server-web:${{ github.sha }}" \
            -t "ghcr.io/michz/shinage-server-web:${{ github.ref_name }}" \
            -f Dockerfile.nginx .
          docker build -t ghcr.io/NAMESPACE/IMAGE_NAME:latest .
          docker images
          docker push -q "ghcr.io/michz/shinage-server-web:${{ github.sha }}"
          docker push -q "ghcr.io/michz/shinage-server-web:${{ github.ref_name }}"
          docker push -q "ghcr.io/michz/shinage-server-app:${{ github.sha }}"
          docker push -q "ghcr.io/michz/shinage-server-app:${{ github.ref_name }}"
#            if [[ "$CI_COMMIT_BRANCH" == "$CI_DEFAULT_BRANCH" ]]; then
#              docker tag "$DOCKER_IMAGE_NAME_APP_SHA" "$DOCKER_IMAGE_NAME_APP_LATEST"
#              docker tag "$DOCKER_IMAGE_NAME_WEB_SHA" "$DOCKER_IMAGE_NAME_WEB_LATEST"
#              docker images
#              docker push -q "$DOCKER_IMAGE_NAME_APP_LATEST"
#              docker push -q "$DOCKER_IMAGE_NAME_WEB_LATEST"
#            fi
